{
  "name": "photo-grid-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon ./server/server.js",
    "install-app": "concurrently \"cd client && npm install\" \"npm install\"",
    "dev": "concurrently \"cd client && npm run start\" \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.3.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^4.1.3",
    "mongoose": "^6.0.11",
    "response-time": "^2.3.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
